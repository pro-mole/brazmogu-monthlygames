-- Global settings and data module
font = {}
sound = {}
bgm = {}
backdrop = {}
sprite = {}
settings = {
	minefield = {
	width = 15,
	height = 15,
	start = {x = 8, y = 15},
	mines = 8,
	coppermoss = "OFF",
	ironcap = "OFF",
	goldendrop = "OFF"
	}
}

function loadSetting(identifier)
end

function saveSetting(identifier, val)
end

function load(slot)
end

function save(slot)
end